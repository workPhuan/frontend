import { ZegoWebSocket } from '../../zego.entity';
import { LogCache } from './index';
import { LogStrategy } from './strategy/log-strategy';
export declare class LogSender {
    private _ws;
    private _cache;
    private _strategy;
    constructor(_ws: ZegoWebSocket, _cache: LogCache, _strategy: LogStrategy);
    active(): void;
}
